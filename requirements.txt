# Core scientific stack
numpy>=1.24,<3.0
pandas>=2.0,<3.0
scikit-learn>=1.3,<2.0
matplotlib>=3.7,<4.0
seaborn>=0.12,<1.0

# PyTorch (CPU build is fine for this project)
torch>=2.1,<3.0
torchvision>=0.16,<1.0  # required for MNIST + CIFAR-10 dataset loaders

# Configuration & experiment tracking
hydra-core>=1.3,<2.0
mlflow>=2.10,<3.0

# API service
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
pydantic>=2.5,<3.0
pydantic-settings>=2.1,<3.0
python-multipart>=0.0.9,<1.0  # required for FastAPI multipart/file uploads

# Deployment (free-tier serverless hosting)
modal>=0.63,<2.0

# Utilities
joblib>=1.3,<2.0
python-json-logger>=2.0,<3.0

# Testing
pytest>=7.4,<9.0
pytest-cov>=4.1,<6.0
httpx>=0.25,<1.0  # TestClient dependency

# Linting
ruff>=0.4,<1.0
